export VAULT_ADDR="https://vault.int.camunda.com"
vault token lookup >/dev/null 2>&1 || { [ -t 1 ] && vault login; }
export destinations=$(vault kv get -field DESTINATIONS -mount="secret" "products/connectors/ci/sap/")
